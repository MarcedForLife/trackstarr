__pycache__/
*.py[cod]
# web/ has its own; this catches a tool run from the repo root by mistake.
node_modules/
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
pending.tsv

# Local development: the .env loaded at startup and the dirs it points at.
.env
!.env.example
dev/

# Machine-specific Claude Code config: the redeploy hook points at
# /opt/stacks/arrs on this host.
.claude/settings.local.json
.claude/hooks/
